#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - count the number of xters in a string
 * @s: string
 * Return: number of xters
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return(len);
}

